# Handoff Document

**Date:** 2024-04-10
**Current State:**
- The repository documentation structure has just been fully populated (`ROADMAP`, `VISION`, `TODO`, `CHANGELOG`, etc.).
- The system is about to be updated to expose ElevenLabs `voice_id` and `model` configuration to the UI and CLI.
- Global versioning from the `VERSION` file is being implemented across the app.

**Next Steps / Unfinished Items:**
- After this cycle, the next model needs to verify the robustness of FFmpeg subtitle burning and investigate adding unit tests for the new parameter parsing logic.
